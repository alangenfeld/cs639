Things to do:
	file struct : add flags
	open: check for flags, and do the appropriate things
	write: update file ptr
	read: 
	readDir: need to write still
	delete: need to write still

passing 8/10 tests
need to pass readDir  and O_CREATE tests
